for _, v in pairs(game:GetService("Players"):GetPlayers()) do
game.Workspace[..tostring(v.Name)].Powers.Quake.ChildAdded:Connect(function(child)

end)
end

game:GetService("Players").PlayerAdded:Connect(function(r)
